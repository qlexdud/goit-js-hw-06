const numberCategories = categories.children.length;
console.log(`Number of categories: ${numberCategories}`)
const lister = document.body.categories;
[...document.getElementsByClassName("categories")].forEach((category) => {
    //const firstListCategory = category.firstElementChild;
    console.log(category);
});